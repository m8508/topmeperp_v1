<div class="page-header">
    <input id="btnCreateTemplate" name="btnCreateTemplate" type="button" class="btn btn-info" value="批次建立詢價單" onclick="createTemplate()" />
    <a href="../downloadAllTemplate?projectid=@ViewBag.projectId" class="btn btn-info" target="_blank">下載所有空白詢價單</a>
    <form id="4formName" name="4formName" action="UpdateFormName" method="post">

        @if (null != Model.planTemplateWithBudget)
        {
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>編號</th>
                        <th>詢價單名稱</th>
                        <th>預算金額</th>
                        <th>報價單數量</th>
                        <th>下載連結</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.planTemplateWithBudget)
                {

                        <tr>
                            <td>
                                <a href="/PurchaseForm/SinglePrjForm/@item.INQUIRY_FORM_ID" target="_blank">@item.INQUIRY_FORM_ID</a>
                            </td>
                            <td>@item.FORM_NAME</td>
                            <td>                                  
                                @String.Format("{0:#,##}", item.BudgetAmount)</td>
                            <td>@item.CountPO</td>
                            <td>
                                <a href="/PurchaseForm/downLoadInquiryForm?formid=@item.INQUIRY_FORM_ID&isTemp=N" target="_blank">下載</a>
                                @*<a href="/PurchaseForm/downLoadInquiryForm4All?formid=@item.INQUIRY_FORM_ID" target="_blank">含工帶料</a>*@
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        }
        else
        {
            <p>尚未建立相關詢價單樣板，透過批次建立詢價單或新增詢價單建立空白詢價單!!</p>
        }
    </form>
</div>
