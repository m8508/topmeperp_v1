<div class="container">
    <h6>請下載需要的詢價單，調整後發給廠商，再行匯入系統!!!</h6>
    <input id="btnCreateTemplate" name="btnCreateTemplate" type="button" class="btn btn-info" value="批次建立詢價單" onclick="createTemplate()" />

    @if (null != Model.planTemplateForm)
    {
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>編號</th>
                    <th>詢價單名稱</th>
                    <th>下載連結</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.planTemplateForm)
                {
                    <tr>
                        <td><a href="/PurchaseForm/SinglePrjForm/@item.INQUIRY_FORM_ID" target="_blank">@item.INQUIRY_FORM_ID</a></td>
                        <td>@item.FORM_NAME</td>
                        <td><a href="/PurchaseForm/downLoadInquiryForm?formid=@item.INQUIRY_FORM_ID" target="_blank">下載</a></td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>尚未建立相關詢價單樣板，透過批次建立詢價單或新增詢價單建立空白詢價單!!</p>
    }
</div>